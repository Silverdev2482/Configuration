exec binds
alias -tla "bind a +moveleft; bind s +back; bind d +forward; bind f +moveright; bind t say_team; bind q slot1; bind w slot2; bind e slot3; bind r slot4; bind c changeclass"
alias +tla "bind t say; bind a sentry; bind s dispencer; bind d telentrance; bind f telexit; bind q ld0; bind w ld1; bind e ld2; bind r ld3; bind c changeteam"

